import 'package:flutter/material.dart';
class SpendingInputPage extends StatelessWidget {
  const SpendingInputPage({super.key});
  @override Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('소비패턴 입력')));
}
