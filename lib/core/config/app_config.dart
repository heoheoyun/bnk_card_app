class AppConfig {
  AppConfig._();
  static const String baseUrl            = 'https://www.bnkcard.store';
  static const Duration connectTimeout   = Duration(seconds: 10);
  static const Duration receiveTimeout   = Duration(seconds: 15);
}
